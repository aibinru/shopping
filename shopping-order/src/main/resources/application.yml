spring:
  application:
    name: shopping-order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: abr123
    url: jdbc:mysql://localhost:3306/shopping_oms?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    type: com.alibaba.druid.pool.DruidDataSource
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-t ype: auto #主键自增

